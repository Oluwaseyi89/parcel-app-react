export const API_BASE = 'http://localhost:7000';


import React from 'react';
export const ApiURL = React.createContext(API_BASE);
export const URL = React.createContext(API_BASE);

export default API_BASE;
